iPXE README File

Quick start guide:

   cd src
   make

For any more detailed instructions, see http://ipxe.org

# matti's additions:

    MAKE_CLEAN=1 IPXE_EMBED=netbootxyz.ipxe IPXE_TARGET=ipxe.usb kung rc ipxe
    MAKE_CLEAN=1 IPXE_EMBED=coreos.ipxe IPXE_TARGET=ipxe.iso kung rc ipxe

    diskutil unmountDisk /dev/disk3
    sudo dd if=src/bin/ipxe.usb of=/dev/rdisk3 bs=1m
